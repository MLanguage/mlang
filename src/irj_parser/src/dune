(env
 (static
  (ocamlopt_flags
   (-O3 -ccopt -static))))

(library
 (name irj_file)
 (public_name irj_parser)
 (wrapped false))

(ocamllex irj_lexer)

(menhir
 (modules irj_parser)
 (flags --strict --explain))
