saisie: 0CF, 1AJ, 1BJ;

const:
V_ANREV = 2018;
V_0AM = 1;

condition:
TSHALLOC >= 10000 et TSHALLOC <= 200000;
TSHALLOV >= 10000 et TSHALLOV <= 200000;
IRNET >= 100;
V_0CF dans (0..10);
((IRNET / (1 + V_0CF / 2))  > 0.1 * (TSHALLOV + TSHALLOC));

sortie: IRNET;
